[project]
name = "wgpy"
version = "0.9.4"
description = "简单使用Wireguard"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=46.0.2",
    "pyinstaller>=6.16.0",
    "pyroute2>=0.9.4",
]
